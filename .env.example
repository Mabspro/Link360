# Supabase (required)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# Required for admin pool create/update and pledge inserts (Supabase → Settings → API → service_role)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Admin: comma-separated list of emails allowed to access /admin (include your Google email for “Sign in with Google”)
LINK360_ADMIN_EMAILS=admin@example.com

# Email (optional – if not set, pledge emails are logged to console)
RESEND_API_KEY=re_xxxx
EMAIL_FROM=Link360 <notifications@yourdomain.com>

# Contact page email (shown on /contact; set to your real inbox)
NEXT_PUBLIC_CONTACT_EMAIL=shipping@link360shipping.com

# Site URL for email links (auto-detected on Vercel; set manually for other hosts)
NEXT_PUBLIC_SITE_URL=https://link360.vercel.app
